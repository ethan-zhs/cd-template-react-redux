import { delay } from 'redux-saga';
import { createModel, updateState } from '@utils/_react_redux_saga_fw/createModel';

export default createModel({
    namespace: 'containers/Login',
    state: {
        // verifyCodeImage: null
    },
    sagas: {
    }
});
